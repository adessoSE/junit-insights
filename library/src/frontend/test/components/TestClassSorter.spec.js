import { mount } from "@vue/test-utils";
import TestClassSorter from "@/components/TestClassSorter.vue";

describe("TestClassSorter", () => {
    it("starts", () => {
        const wrapper = mount(TestClassSorter)
    })
})
